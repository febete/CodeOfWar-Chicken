using System.Collections;
using System.Collections.Generic;
using Berkay;
using UnityEngine;

public class WorkerWormEater : MonoBehaviour
{

    /*[SerializeField] private WorkerChickenAI workerChickenAI;
    [SerializeField] private Transform beak;
    [SerializeField] private workerChickenSO workerChickenStat;
    public List<GameObject> WormsInMount = new List<GameObject>();
    
    private void OnTriggerEnter(Collider other)
    {
        if (other.TryGetComponent(out Hole hole))
        {
            hole.fakeWorm.transform.SetParent(beak);
            hole.fakeWorm.localPosition = Vector3.zero;
            WormsInMount.Add(hole.fakeWorm.gameObject);
            Destroy(hole.gameObject);
            workerChickenAI.workerState = WorkerState.GoingMother;
        }
        

    }*/
}
